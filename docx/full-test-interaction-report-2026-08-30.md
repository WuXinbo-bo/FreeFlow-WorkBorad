# FreeFlow 全量测试与交互验收报告

## 1. 验收结论

- 验收日期：2026-08-30（Asia/Shanghai）
- 验收基线：`main` / `a9fe396571610c3ea912ad7381bce868cdd04ad0`
- 最终结论：通过。本轮发现并修复 1 项测试脚本状态机过期问题；未发现需要修改产品实现的阻断性缺陷。
- Git 起始状态：工作区干净，本地 `main` 比 `origin/main` 领先 20 个提交。
- 报告口径：本报告只陈述本轮实际执行和观察到的结果，不把未执行项目记为通过。

## 2. 执行范围

本轮按以下顺序验收当前代码：

1. 检查 Git 基线和正式测试入口。
2. 两次运行项目官方全量测试 `npm test`，修改前和修改后均要求退出码为 0。
3. 补跑官方套件未纳入的发布、升级、持久化、导出和浏览器专项检查。
4. 独立启动 Web 应用，实际操作菜单、设置抽屉、右侧工作区、缩放和恢复入口。
5. 检查进入态、连续操作、退出恢复、快速重复进入/退出和相邻功能。
6. 复核截图与 DOM 边界，检查窄视口的遮挡、溢出和动画稳定态。

未执行范围：未生成 Windows 安装包，未进行真实外部 AI 服务调用，未进行长时间稳定性或真实多设备人工测试。这些项目不计入“通过”。

## 3. 官方全量测试

命令：`npm test`

最终结果：退出码 0，输出 `[test] all checks passed`。

通过内容：

- 构建产物一致性检查。
- 后端安全、桌面原子保存和 Electron IPC 安全检查。
- Canvas LOD 与 overlay 预算检查。
- 9 组结构化解析器验证。
- 22 组渲染器与元素集成验证。
- Canvas2D 浏览器回归，包括富文本、代码、数学、表格、平移、拖拽、框选、局部 tile 失效、低缩放 LOD、overlay 恢复、撤销、粘贴语义、剪贴板和思维导图操作。
- Air Canvas 交互检查，覆盖 `1600x1000`、`900x900` 和 `680x820` 三种视口。

恢复链实测包含：

- 表格编辑退出后，编辑器与工具栏隐藏，代码 overlay 恢复；快速循环 3 次正常。
- 富文本内容在低缩放隐藏后，两次恢复到相同缩放阈值，内容与尺寸保持一致。
- 平移和选择拖拽在释放后完成提交，连续拖拽仍更新位置。
- 设置抽屉快速开关后，遮罩 `pointer-events` 恢复为 `none`。
- AI 助手与 AI 镜像重复切换后，输入区、映射面板和标题区恢复正确。

浏览器回归中的代表性观测值（仅为本机本次样本，不作为正式性能基准）：

- 平移交互帧耗时：拖动中约 1.6 ms，提交后约 1.5 ms。
- 选择拖拽帧耗时：拖动中约 0.4 ms，提交后约 0.5 ms。
- 4000 个元素的视口裁剪样本：渲染 80 个，裁剪 3920 个。
- 低缩放 3 元素 LOD 样本：3 个元素均进入简化渲染；该次帧耗时约 32.1 ms。

## 4. 补充专项检查

以下检查均实际运行并通过：

- 桌面打包前静态检查。
- 发布版本一致性：`v1.2.0`。
- 四种桌面升级兼容场景：最近画布保留、教程回退、旧用户模板刷新、当前版本介绍关闭状态。
- 最近画布持久化与启动解析。
- UI 习惯迁移、启动偏好持久化和旧项目设置迁移。
- 工作台偏好 API 路由。
- 导出图片资源回填：本地、远程、内联资源成功，缺失资源按预期不回填。
- Word 选区导出：代码、文本、表格和公式顺序正确，图片按不支持类型跳过，生成缓冲区 10519 字节。
- 画布搜索覆盖：节点、表格、代码及富文本内嵌表格/代码均可命中。
- 粘贴性能专项：纯文本约 57.1 ms，多行文本约 58.5 ms；121 元素场景粘贴约 104 ms，视图仍可响应且无页面错误。
- 思维导图关系连接线专项。
- 富文本工具栏布局、选区工具栏和恢复链专项。

## 5. 发现与处理

### 5.1 富文本工具栏专项测试误报

首次补跑 `check-rich-toolbar-layout.js` 时失败，报告“常驻工具栏不可见”。诊断确认产品行为正常，失败原因是测试仍假设“新增思维节点后需要双击进入编辑”。当前行为是新增节点即进入编辑，旧脚本再次双击会退出编辑，因此产生误报。

处理：

- 先读取当前 `editingId`，仅在尚未进入目标节点编辑态时双击。
- 补充视图缩放和偏移换算，避免使用未经转换的画布坐标。
- 选区覆盖整个富文本编辑器，避免依赖首个子节点结构。
- 单行布局改为比较直接子控件的垂直中心，不再把不同高度色块或嵌套弹层误判为换行。
- 新增 `Escape` 退出恢复检查和 3 次快速进入/退出检查。

修正后结果：常驻工具栏可见且 `flex-wrap: nowrap`；所有直接子控件垂直中心一致；选区工具栏可见且两行位置分别为 13 px 和 47 px；退出与 3 次快速循环后 `editingId` 清空，两个工具栏均隐藏。

### 5.2 独立交互脚本的两次诊断性超时

- 第一次把分享菜单错误地按 `is-hidden` 类判断，实际组件使用可见性切换。改用真实可见性后通过。
- 第二次直接点击被持久化偏好隐藏的右侧面板标签。改为先点击“恢复右侧面板”，再执行切换后通过。

这两项是临时验收脚本前提错误，不是产品缺陷，未伪记为产品失败或通过。

### 5.3 设置抽屉截图时机复核

680x820 视口的第一张截图在抽屉 200 ms 滑入动画起始帧采集，只显示右侧边缘。等待 260 ms 后重新量测：抽屉边界为左 248、上 12、右 668、下 808，完整位于视口内；关闭动画完成后抽屉移出视口，遮罩恢复为不可点击。稳定帧视觉检查未发现不可用遮挡。

## 6. 独立交互体验结果

桌面视口 `1440x960`：

- 从持久化的右面板隐藏态进入，经恢复按钮正确显示右面板。
- AI 助手与 AI 镜像连续切换 3 次，最终恢复到助手态。
- 初始缩放 75%，点击放大后为 84%，重置后恢复 75%，适配内容可执行。
- 最终无页面横向溢出，映射面板关闭，聊天输入区可见，浏览器错误列表为空。

窄视口 `680x820`：

- 分享菜单连续开关 3 次正常。
- 设置抽屉连续开关 3 次正常。
- 会话菜单点击外部后关闭。
- 最终无横向溢出、无残留菜单、无残留抽屉、遮罩不可点击，浏览器错误列表为空。
- 稳定帧截图人工复核：顶部工具栏、画布目录、缩放条、设置抽屉及其文本均可见，未发现非预期重叠。

## 7. 残余风险与建议

- Word 选区导出检查通过，但 Node 输出 `fs.F_OK` 已弃用警告。该警告不影响本轮结果，建议后续单独追踪，不在本次验收中顺带修改无关实现。
- 官方 `npm test` 没有直接包含全部独立 `check-*.js`；本轮已人工补跑相关脚本。后续可单独评估是否将稳定且无环境副作用的专项检查纳入统一入口。
- 本轮性能数字来自单次本机回归样本，只能证明阈值内行为正常，不能替代多轮统计基准。
- 外部 AI 服务质量、安装包安装/升级和长时间运行仍需发布阶段验收。

## 8. 最终验收清单

- [x] 进入交互态正常。
- [x] 交互中连续操作正常。
- [x] 退出交互态后恢复正常。
- [x] 快速重复进入/退出后仍正常。
- [x] 相关相邻功能未发现连带回归。
- [x] 官方全量测试在最终改动后通过。
- [x] 补充发布与浏览器专项检查通过。
- [x] Git 差异检查无空白错误。
- [x] 报告内容集中在本文件。
