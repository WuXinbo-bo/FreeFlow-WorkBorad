# ND-001 旧节点混用样本

## 样本目的

用于观察旧 `flow` 节点体系与新结构化内容共存时的复制、粘贴、拖拽与选中行为。

## 当前状态

- `flow` 节点拥有自己相对独立的原生元素与文本体系

## 目标状态

- 第一阶段不强制把 `flow` 节点升级为 canonical 原生节点
- 通过 adapter 进入兼容管理层
- 确保与新结构内容混用时不冲突

## 说明

本样本属于“兼容观察对象”。
